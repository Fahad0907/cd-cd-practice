name: Deploy Django App

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Deploy to Private EC2
        run: |
          ssh ubuntu@10.0.2.14 << 'EOF'

            cd /var/www/cd-cd-practice

            git pull origin main

          EOF

      - name: Notify Success
        run: echo "Deployment Successful!"
